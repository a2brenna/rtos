cmdline options for test to switch between backends
script to run perf ./test for different backends
Add correctness mode that runs different backends against each other
    Add tests for append correctness
Transaction logging
